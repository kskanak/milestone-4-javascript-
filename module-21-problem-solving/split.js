let text = "no hard feelings yaar. not at all. no problem";

// between quatation empty space part aways the words and return the word into a array
let word = text.split(" ");
console.log(word);

// between quation using dot means, after the dot starts a new line as sentence

let sentence = text.split(".");
console.log(sentence);

// just quatation split the characters;

let characters = text.split("");
console.log(characters);
