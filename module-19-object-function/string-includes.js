var names = "arina grande talks in monday";

var checks = names.includes("sun");
console.log(checks);

// substrings
var names = "arina grande talks in monday";
var checkSubstrings = names.substring(0, 9);
console.log(checkSubstrings);
